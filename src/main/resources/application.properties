# Configuración de la base de datos en Azure
spring.datasource.url=jdbc:mysql://fellaslibrery.mysql.database.azure.com:3306/librosbd?useSSL=true&requireSSL=true
spring.datasource.username=calperfella
spring.datasource.password=Derekjeter51

# Configuración del driver MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true

# Configuración del pool de conexiones
spring.datasource.hikari.maximum-pool-size=10

# Opcional: Configuración para depuración
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE

